import React from "react";
import PillarQuestions from "@/components/organisms/PillarQuestions";

const PillarPage = ({ currentUser }) => {
  return <PillarQuestions currentUser={currentUser} />;
};

export default PillarPage;